spring.application.name=graphql-example

spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=postgres
spring.datasource.password=0504

# PostgreSQL-e uygun dialekti teyin edir
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Verilenler bazasini yenilemek ���n (update evezine create, create-drop, validate istifade ede bilersiniz)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# gRPC Client Configuration for Order Service
grpc.client.orderService.address=static://localhost:9090
grpc.client.orderService.negotiationType=plaintext

